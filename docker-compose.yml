services:
  scraper:
    build: .
    container_name: mac-scraper
    env_file: .env
    volumes:
      - scraper-data:/data
      - ./filter_rules.json:/app/filter_rules.json:ro
    restart: unless-stopped

volumes:
  scraper-data:
